        assert(bank[argMax+1]!=)
        print("new_space is", new_space)
        sol.append(max(new_space))
        print(argMax, (batteries-i+1))
        argMax = argmax(bank[:-(batteries-i+1)], argMax+1)
        print("argMax is", argMax)
        #print(new_space, argMax)
        print(sol)
    return int(reduce(lambda acc,elem: str(elem)+acc, reversed(sol),""))